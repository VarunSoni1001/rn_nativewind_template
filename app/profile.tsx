import { View, Text } from "react-native";
import React from "react";

const profile = () => {
  return (
    <View className="flex-1 items-center justify-center bg-slate-200">
      <Text className="font-psemibold">profile</Text>
    </View>
  );
};

export default profile;
